import React from 'react';
import { DraggablePropsType } from './Draggable.types';

const Draggable = ({ children }: DraggablePropsType) => {
  return <div>{children}</div>;
};

export default Draggable;
